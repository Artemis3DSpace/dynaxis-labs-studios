---
id: WP-7C-15
phase: 7C
title: Route Migration: Generations Jobs and Lifecycle
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-10", "WP-7C-13"]
blocks: ["WP-7C-23"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["app/api/dynaxis/generations/**", "app/api/dynaxis/jobs/**", "app/api/dynaxis/lifecycle/**", "lib/dynaxis/services/generations.js", "lib/dynaxis/services/jobs.js", "lib/dynaxis/services/lifecycle.js", "tests/dynaxis-provider-kernel.test.mjs"]
---

# Objective

Deliver Route Migration: Generations Jobs and Lifecycle for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Route Migration: Generations Jobs and Lifecycle without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-10
- WP-7C-13

# Allowed Paths

- app/api/dynaxis/generations/**
- app/api/dynaxis/jobs/**
- app/api/dynaxis/lifecycle/**
- lib/dynaxis/services/generations.js
- lib/dynaxis/services/jobs.js
- lib/dynaxis/services/lifecycle.js
- tests/dynaxis-provider-kernel.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- Generations, Jobs, and lifecycle routes migrated to AuthContext and project permission checks
- Generation Gateway and lifecycle calls receive canonical project/workspace context without making providers identity authorities
- status/provider-id/complete/fail routes preserve existing provider-kernel compatibility and add denied-by-default authorization

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Generation and Job routes enforce project access before returning or mutating execution records.
- Lifecycle routes preserve provider-neutral Job/Generation invariants and reject unauthorized completion/failure updates.

# Validation

- provider-kernel and lifecycle regression tests with authorized, forbidden, stale-job, and missing-project cases
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
