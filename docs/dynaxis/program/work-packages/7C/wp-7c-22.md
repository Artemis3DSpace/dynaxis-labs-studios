---
id: WP-7C-22
phase: 7C
title: Session Rate Limit Abuse and Security Tests
status: done
agent: claude
base_sha: ef80ee31056b3383238e9cb90772964104940cd4
branch: phase-7c/session-rate-limit-abuse-security-tests
worktree: dynaxis-labs-studios
depends_on: ["WP-7C-11", "WP-7C-21"]
blocks: ["WP-7C-23"]
migration_owner: false
type: review
recommended_agent: claude
shared_files: ["tests/dynaxis-auth-foundation.test.mjs", "tests/dynaxis-workspace-foundation.test.mjs", "tests/dynaxis-authorization*.test.mjs"]
---

# Objective

Deliver Session Rate Limit Abuse and Security Tests for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Session Rate Limit Abuse and Security Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-11
- WP-7C-21

# Allowed Paths

- tests/dynaxis-auth-foundation.test.mjs
- tests/dynaxis-workspace-foundation.test.mjs
- tests/dynaxis-authorization*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- review and test evidence for Session Rate Limit Abuse, Security Tests
- coverage matrix mapping each reviewed implementation package to acceptance criteria
- recorded defects, residual risks, and required fixes before integration
- Review must validate implementation against programme contracts.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Review depends on all implementations it evaluates.
- No reviewed package can pass without tests or documented evidence for its core invariants.

# Validation

- run or specify targeted validation for every reviewed implementation package
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
