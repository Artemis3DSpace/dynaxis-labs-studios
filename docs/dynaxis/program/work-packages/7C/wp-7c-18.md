---
id: WP-7C-18
phase: 7C
title: TanStack Query Foundation and Query Keys
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-12"]
blocks: ["WP-7C-19"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["packages/studio/src/**", "lib/dynaxis/client/platform-api.js", "lib/dynaxis/client/project-context.js"]
---

# Objective

Deliver TanStack Query Foundation and Query Keys for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver TanStack Query Foundation and Query Keys without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-12

# Allowed Paths

- packages/studio/src/**
- lib/dynaxis/client/platform-api.js
- lib/dynaxis/client/project-context.js
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- TanStack Query client foundation with stable query-key factory for session, workspace, project, asset, generation, and domain resources
- client API wrapper behavior for auth failures, forbidden responses, stale cache invalidation, and optimistic rollback boundaries
- no TanStack Router or TanStack Start introduction

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Query keys are centralized and deterministic across Studio surfaces.
- The package adds TanStack Query foundation only; it does not migrate every Studio workflow.

# Validation

- client unit tests or focused UI smoke tests for query keys, cache invalidation, and auth error handling
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
