---
id: WP-7C-13
phase: 7C
title: AuthContext Route Helper Integration
status: in_progress
agent: codex
base_sha: 373ab597b0ee66359d490b37876478ce8075cb50
branch: phase-7c/auth-context-route-helper
worktree: dynaxis-labs-studios-phase-7c-auth-context-route-helper
depends_on: ["WP-7C-12"]
blocks: ["WP-7C-14", "WP-7C-15", "WP-7C-16", "WP-7C-17"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/auth/route-context.js", "lib/dynaxis/api.js", "tests/dynaxis-auth-context*.test.mjs"]
---

# Objective

Deliver AuthContext Route Helper Integration for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver AuthContext Route Helper Integration without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-12

# Allowed Paths

- lib/dynaxis/auth/route-context.js
- lib/dynaxis/api.js
- tests/dynaxis-auth-context*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- route helper functions that adapt Next.js `Request` to AuthContext
- standard JSON error response mapping for authn/authz failures
- migration guide for replacing `requireOwnerFromRequest` per route family
- Do not migrate all routes in this package; provide the route boundary used by later route packages
- Keep legacy API-key compatibility explicit and auditable

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Later route packages can remove direct `x-api-key` parsing from their route files.
- Route errors are consistent and do not leak cross-workspace resource existence.

# Validation

- helper tests for GET/POST request flows and error mapping
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
