---
id: WP-7C-19
phase: 7C
title: Client Session and Workspace Switching
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-12", "WP-7C-18"]
blocks: ["WP-7C-20"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["packages/studio/src/**", "lib/dynaxis/auth/client.js", "lib/dynaxis/client/project-context.js"]
---

# Objective

Deliver Client Session and Workspace Switching for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Client Session and Workspace Switching without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-12
- WP-7C-18

# Allowed Paths

- packages/studio/src/**
- lib/dynaxis/auth/client.js
- lib/dynaxis/client/project-context.js
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- client session integration using Better Auth client/session state and active organization from the server session
- workspace switching flow that updates active Better Auth organization and invalidates workspace/project queries
- personal workspace behavior remains exactly-one provisioned workspace and cannot be deleted or shared through this UI

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Switching workspace invalidates project/resource queries and reloads project context deterministically.
- Client state does not introduce a second active-workspace cookie or localStorage authority.

# Validation

- client/session tests for sign-in, missing session, workspace switch, and personal workspace protections
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
