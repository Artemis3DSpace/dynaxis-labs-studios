---
id: WP-7C-09
phase: 7C
title: Authorization Evaluator and Workspace Policy
status: done
agent: codex
base_sha: null
branch: phase-7c/authorization-workspace-policy
worktree: null
depends_on: ["WP-7C-08"]
blocks: ["WP-7C-10", "WP-7C-11"]
migration_owner: false
type: implementation
recommended_agent: claude
shared_files: ["lib/dynaxis/auth/permissions.js", "lib/dynaxis/auth/policy.js", "lib/dynaxis/auth/workspace-policy.js", "lib/dynaxis/auth/workspace-access.js", "tests/dynaxis-authorization*.test.mjs"]
---

# Objective

Deliver Authorization Evaluator and Workspace Policy for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Authorization Evaluator and Workspace Policy without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-08

# Allowed Paths

- lib/dynaxis/auth/permissions.js
- lib/dynaxis/auth/policy.js
- lib/dynaxis/auth/workspace-policy.js
- lib/dynaxis/auth/workspace-access.js
- tests/dynaxis-authorization*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- server-only policy evaluator with explicit allow/deny result shape
- workspace policy adapter for Better Auth organization roles
- permission constants generated or exported from the 7C.6 vocabulary
- Deny by default for unknown permission, missing subject, inactive session, or missing workspace
- Treat Better Auth organization membership as workspace membership only, not project membership
- Return structured failure codes without leaking membership of other workspaces

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Policy evaluator returns deterministic results for every workspace role.
- No project-resource inheritance is implemented in this package.

# Validation

- workspace owner/admin/member/viewer allow/deny matrix
- missing session and stale active organization tests
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
