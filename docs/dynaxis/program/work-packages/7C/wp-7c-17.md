---
id: WP-7C-17
phase: 7C
title: Route Migration: Design APIs and Mini App Execution
status: review
agent: codex
base_sha: 4f496ff0fcd19a8505b17fce0a9843834143b25e
branch: phase-7c/route-migration-design-mini-app
worktree: dynaxis-labs-studios-phase-7c-route-migration-design-mini-app
depends_on: ["WP-7C-13"]
blocks: ["WP-7C-23"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["app/api/dynaxis/compositions/**", "app/api/dynaxis/design-agent/**", "app/api/dynaxis/design-components/**", "app/api/dynaxis/design-component-sets/**", "app/api/dynaxis/design-systems/**", "app/api/dynaxis/design-templates/**", "lib/dynaxis/services/compositions.js", "lib/dynaxis/services/design-agent.js"]
---

# Objective

Deliver Route Migration: Design APIs and Mini App Execution for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Route Migration: Design APIs and Mini App Execution without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-13

# Allowed Paths

- app/api/dynaxis/compositions/**
- app/api/dynaxis/design-agent/**
- app/api/dynaxis/design-components/**
- app/api/dynaxis/design-component-sets/**
- app/api/dynaxis/design-systems/**
- app/api/dynaxis/design-templates/**
- lib/dynaxis/services/compositions.js
- lib/dynaxis/services/design-agent.js
- lib/dynaxis/services/components.js
- lib/dynaxis/services/design-systems.js
- lib/dynaxis/services/templates.js
- tests/dynaxis-compositions.test.mjs
- tests/dynaxis-design-*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- Composition, Design Agent, Design Components, Component Sets, Design Systems, and Design Templates routes migrated to AuthContext
- Mini App execution boundaries use canonical project/workspace permission checks before generation or design mutations
- Design Agent context assembly respects project authorization and does not expose unrelated workspace resources

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Design APIs reject unauthorized cross-project reads/writes and preserve existing Composition/Design revision behavior.
- Mini App execution cannot bypass AuthContext through legacy client state.

# Validation

- composition, design-agent, design-components, design-systems, templates, and mini-app host tests
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
