---
id: WP-7C-23
phase: 7C
title: Identity Integration Gate
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-07", "WP-7C-11", "WP-7C-14", "WP-7C-15", "WP-7C-16", "WP-7C-17", "WP-7C-20", "WP-7C-22"]
blocks: ["WP-7D-03", "WP-7F-02", "WP-7I-02", "WP-8A-02", "WP-8E-02", "WP-8F-02", "WP-10-01", "WP-10-05"]
migration_owner: false
type: integration
recommended_agent: codex
shared_files: ["tests/dynaxis*.test.mjs"]
---

# Objective

Deliver Identity Integration Gate for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Identity Integration Gate without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-07
- WP-7C-11
- WP-7C-14
- WP-7C-15
- WP-7C-16
- WP-7C-17
- WP-7C-20
- WP-7C-22

# Allowed Paths

- docs/dynaxis/program/**
- tests/dynaxis*.test.mjs

# Forbidden Paths

- new feature implementation
- schema expansion beyond already reviewed packages
- merge actions without user approval
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- integration gate evidence for Identity Integration Gate
- dependency and conflict closure checklist
- release notes for contracts, migrations, validation, and residual risk
- Integration gate must not introduce unreviewed implementation.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- The gate depends on every component it integrates.
- No merge is performed by the agent running the gate.

# Validation

- run phase-level validation commands listed by component packages
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
