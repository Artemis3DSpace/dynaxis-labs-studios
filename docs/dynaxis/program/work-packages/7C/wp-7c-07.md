---
id: WP-7C-07
phase: 7C
title: Project Membership Tests and Fixtures
status: done
agent: codex
base_sha: null
branch: phase-7c/project-membership-review
worktree: dynaxis-labs-studios-phase-7c-project-membership-review
depends_on: ["WP-7C-05", "WP-7C-06"]
blocks: ["WP-7C-23"]
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["tests/dynaxis-project-membership*.test.mjs", "tests/dynaxis-workspace-foundation.test.mjs"]
---

# Objective

Deliver Project Membership Tests and Fixtures for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Project Membership Tests and Fixtures without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-05
- WP-7C-06

# Allowed Paths

- tests/dynaxis-project-membership*.test.mjs
- tests/dynaxis-workspace-foundation.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- project membership fixtures covering personal workspace and organization workspace projects
- regression matrix for project roles vs workspace roles
- migration inspection tests for the membership table
- Review membership behavior only; do not add policy evaluator behavior here

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Every project role has positive and negative fixture coverage.
- Tests prove project membership references the canonical Project, not a duplicate project entity.

# Validation

- run targeted membership tests plus workspace foundation/ownership tests
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
