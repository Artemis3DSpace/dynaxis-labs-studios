---
id: WP-7C-16
phase: 7C
title: Route Migration: Characters Products Brands Campaigns
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-13"]
blocks: ["WP-7C-23"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["app/api/dynaxis/characters/**", "app/api/dynaxis/products/**", "app/api/dynaxis/brands/**", "app/api/dynaxis/campaigns/**", "lib/dynaxis/services/characters.js", "lib/dynaxis/services/products.js", "lib/dynaxis/services/brands.js", "lib/dynaxis/services/campaigns.js"]
---

# Objective

Deliver Route Migration: Characters Products Brands Campaigns for Phase 7C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Dynaxis owns Projects, Jobs, Assets, identity, graph, memory, orchestration, and verification. This Work Package is intentionally narrow: it must deliver Route Migration: Characters Products Brands Campaigns without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-13

# Allowed Paths

- app/api/dynaxis/characters/**
- app/api/dynaxis/products/**
- app/api/dynaxis/brands/**
- app/api/dynaxis/campaigns/**
- lib/dynaxis/services/characters.js
- lib/dynaxis/services/products.js
- lib/dynaxis/services/brands.js
- lib/dynaxis/services/campaigns.js
- tests/dynaxis-characters.test.mjs
- tests/dynaxis-products.test.mjs
- tests/dynaxis-brands.test.mjs
- tests/dynaxis-campaigns.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Provider Connections / Secrets runtime
- Developer Platform API credentials
- another Project entity
- Better Auth replacement

# Required Deliverables

- Character, Product, Brand, and Campaign route families migrated to AuthContext
- domain services receive canonical workspace/project context while preserving existing revision and asset-link behavior
- project association routes enforce membership and resource inheritance without duplicating ownership fields on child rows

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- CRUD and revision routes for Characters, Products, Brands, and Campaigns are protected by the correct project/workspace policy.
- Existing domain revision and relationship invariants continue to pass after route migration.

# Validation

- targeted route/domain tests for characters, products, brands, and campaigns
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
