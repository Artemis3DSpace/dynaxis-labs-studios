---
id: WP-7H-04
phase: 7H
title: Soul ID Attachment and Open Provider Adapters
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7H-03"]
blocks: ["WP-7H-05"]
migration_owner: false
type: implementation
recommended_agent: claude
shared_files: ["lib/dynaxis/identity-profiles/**", "lib/dynaxis/characters/**", "lib/dynaxis/providers/**", "app/api/dynaxis/characters/**", "packages/mini-apps/character-studio/**", "tests/dynaxis-character-identity*.test.mjs"]
---

# Objective

Deliver Soul ID Attachment and Open Provider Adapters for Phase 7H. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Character Identity Profiles attach provider identities such as Soul ID to Dynaxis Characters; the Character remains canonical. This Work Package is intentionally narrow: it must deliver Soul ID Attachment and Open Provider Adapters without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7H-03

# Allowed Paths

- lib/dynaxis/identity-profiles/**
- lib/dynaxis/characters/**
- lib/dynaxis/providers/**
- app/api/dynaxis/characters/**
- packages/mini-apps/character-studio/**
- tests/dynaxis-character-identity*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- Soul ID provider-profile attachment contract and mapping to Character identity profiles
- open adapter interface for future identity providers without provider-specific Character schema changes
- routing behavior selecting usable identity profile for a generation request
- Character Identity Profiles attach provider identities such as Soul ID to Dynaxis Characters; the Character remains canonical. This Work Package is intentionally narrow: it must deliver Soul ID Attachment and Open Provider Adapters without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Soul ID provider-profile attachment contract and mapping to Character identity profiles is implemented, documented, or verified within this package.
- open adapter interface for future identity providers without provider-specific Character schema changes is implemented, documented, or verified within this package.

# Validation

- tests for Soul ID attach/update/revoke and fallback to Dynaxis reference identity
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
