---
id: WP-8B-07
phase: 8B
title: Build Runtime Evaluations
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8B-02", "WP-8B-03", "WP-8B-04", "WP-8B-05", "WP-8B-06"]
blocks: []
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/build-runtime/**", "lib/dynaxis/repositories/**", "lib/dynaxis/engineering/**", "app/api/dynaxis/build/**", "tests/dynaxis-build-runtime*.test.mjs"]
---

# Objective

Deliver Build Runtime Evaluations for Phase 8B. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Build Runtime turns briefs into verified engineering work packages and repository execution without bypassing contracts or GitHub authority. This Work Package is intentionally narrow: it must deliver Build Runtime Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8B-02
- WP-8B-03
- WP-8B-04
- WP-8B-05
- WP-8B-06

# Allowed Paths

- lib/dynaxis/build-runtime/**
- lib/dynaxis/repositories/**
- lib/dynaxis/engineering/**
- app/api/dynaxis/build/**
- tests/dynaxis-build-runtime*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- evaluation suite for intake-to-plan-to-work-package-to-repository execution flow
- quality metrics for requirement coverage, package granularity, verification completeness, and repair-loop convergence
- failure corpus for ambiguous briefs, incompatible components, and unsafe repository operations
- Build Runtime turns briefs into verified engineering work packages and repository execution without bypassing contracts or GitHub authority. This Work Package is intentionally narrow: it must deliver Build Runtime Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- evaluation suite for intake-to-plan-to-work-package-to-repository execution flow is implemented, documented, or verified within this package.
- quality metrics for requirement coverage, package granularity, verification completeness, and repair-loop convergence is implemented, documented, or verified within this package.

# Validation

- review report before any Supercomputer-level orchestration consumes Build Runtime
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
