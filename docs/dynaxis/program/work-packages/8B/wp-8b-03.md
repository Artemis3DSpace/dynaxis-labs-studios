---
id: WP-8B-03
phase: 8B
title: Engineering Work Package Generation
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7I-05", "WP-8A-07", "WP-8B-02"]
blocks: ["WP-8B-04", "WP-8B-07"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/build-runtime/**", "lib/dynaxis/repositories/**", "lib/dynaxis/engineering/**", "app/api/dynaxis/build/**", "tests/dynaxis-build-runtime*.test.mjs"]
---

# Objective

Deliver Engineering Work Package Generation for Phase 8B. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Build Runtime turns briefs into verified engineering work packages and repository execution without bypassing contracts or GitHub authority. This Work Package is intentionally narrow: it must deliver Engineering Work Package Generation without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7I-05
- WP-8A-07
- WP-8B-02

# Allowed Paths

- lib/dynaxis/build-runtime/**
- lib/dynaxis/repositories/**
- lib/dynaxis/engineering/**
- app/api/dynaxis/build/**
- tests/dynaxis-build-runtime*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- generator that emits scoped engineering WorkPackages from architecture plan, App IR delta, repository state, and verification requirements
- dependency graph and allowed/forbidden path computation for generated packages
- human-review stop condition for high-risk, ambiguous, or destructive work
- Build Runtime turns briefs into verified engineering work packages and repository execution without bypassing contracts or GitHub authority. This Work Package is intentionally narrow: it must deliver Engineering Work Package Generation without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- generator that emits scoped engineering WorkPackages from architecture plan, App IR delta, repository state, and verification requirements is implemented, documented, or verified within this package.
- dependency graph and allowed/forbidden path computation for generated packages is implemented, documented, or verified within this package.

# Validation

- tests for generated package granularity, dependency correctness, and validation completeness
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
