---
id: WP-8G-05
phase: 8G
title: Plugin Platform Security Review
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8G-02", "WP-8G-03", "WP-8G-04"]
blocks: ["WP-8H-02"]
migration_owner: false
type: review
recommended_agent: claude
shared_files: ["lib/dynaxis/plugins/**", "lib/dynaxis/extensions/**", "lib/dynaxis/skills/**", "app/api/dynaxis/plugins/**", "tests/dynaxis-plugin*.test.mjs"]
---

# Objective

Deliver Plugin Platform Security Review for Phase 8G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Plugin Platform Security Review without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8G-02
- WP-8G-03
- WP-8G-04

# Allowed Paths

- lib/dynaxis/plugins/**
- lib/dynaxis/extensions/**
- lib/dynaxis/skills/**
- app/api/dynaxis/plugins/**
- tests/dynaxis-plugin*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- security review covering manifest permissions, sandbox escape, signing, dependency integrity, lifecycle rollback, and extension-point abuse
- negative tests for unauthorized filesystem/network/API access where applicable
- evidence that plugins register canonical Dynaxis objects instead of hidden product models
- Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Plugin Platform Security Review without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- security review covering manifest permissions, sandbox escape, signing, dependency integrity, lifecycle rollback, and extension-point abuse is implemented, documented, or verified within this package.
- negative tests for unauthorized filesystem/network/API access where applicable is implemented, documented, or verified within this package.

# Validation

- residual risk report before Marketplace distributes plugins
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
