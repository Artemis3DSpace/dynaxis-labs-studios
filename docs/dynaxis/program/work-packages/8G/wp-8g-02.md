---
id: WP-8G-02
phase: 8G
title: Plugin SDK Runtime Sandbox and Extension Points
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8E-05", "WP-8F-06", "WP-8G-01"]
blocks: ["WP-8G-03", "WP-8G-05"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/plugins/**", "lib/dynaxis/extensions/**", "lib/dynaxis/skills/**", "app/api/dynaxis/plugins/**", "tests/dynaxis-plugin*.test.mjs"]
---

# Objective

Deliver Plugin SDK Runtime Sandbox and Extension Points for Phase 8G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Plugin SDK Runtime Sandbox and Extension Points without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8E-05
- WP-8F-06
- WP-8G-01

# Allowed Paths

- lib/dynaxis/plugins/**
- lib/dynaxis/extensions/**
- lib/dynaxis/skills/**
- app/api/dynaxis/plugins/**
- tests/dynaxis-plugin*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- Plugin SDK for manifest loading, capability registration, extension-point binding, and host communication
- runtime sandbox enforcing declared permissions, resource limits, and allowed extension points
- failure behavior for denied permission, incompatible host version, runtime crash, and timeout
- Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Plugin SDK Runtime Sandbox and Extension Points without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Plugin SDK for manifest loading, capability registration, extension-point binding, and host communication is implemented, documented, or verified within this package.
- runtime sandbox enforcing declared permissions, resource limits, and allowed extension points is implemented, documented, or verified within this package.

# Validation

- tests for sandbox isolation and extension-point registration
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
