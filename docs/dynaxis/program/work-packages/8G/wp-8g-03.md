---
id: WP-8G-03
phase: 8G
title: Signing Integrity Dependency and Compatibility Model
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8G-02"]
blocks: ["WP-8G-04", "WP-8G-05"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/plugins/**", "lib/dynaxis/extensions/**", "lib/dynaxis/skills/**", "app/api/dynaxis/plugins/**", "tests/dynaxis-plugin*.test.mjs"]
---

# Objective

Deliver Signing Integrity Dependency and Compatibility Model for Phase 8G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Signing Integrity Dependency and Compatibility Model without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8G-02

# Allowed Paths

- lib/dynaxis/plugins/**
- lib/dynaxis/extensions/**
- lib/dynaxis/skills/**
- app/api/dynaxis/plugins/**
- tests/dynaxis-plugin*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- signing and integrity verification model for plugin packages and manifests
- dependency/version compatibility resolver for plugin host, Skills, capabilities, and SDK versions
- install-time rejection behavior for unsigned, tampered, incompatible, or cyclic dependency packages
- Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Signing Integrity Dependency and Compatibility Model without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- signing and integrity verification model for plugin packages and manifests is implemented, documented, or verified within this package.
- dependency/version compatibility resolver for plugin host, Skills, capabilities, and SDK versions is implemented, documented, or verified within this package.

# Validation

- tests for signature verification and dependency conflict reports
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
