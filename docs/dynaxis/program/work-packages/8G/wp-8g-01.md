---
id: WP-8G-01
phase: 8G
title: Plugin Manifest Package Permissions and Capabilities
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: []
blocks: ["WP-8G-02"]
migration_owner: false
type: specification
recommended_agent: codex
shared_files: []
---

# Objective

Deliver Plugin Manifest Package Permissions and Capabilities for Phase 8G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries. This Work Package is intentionally narrow: it must deliver Plugin Manifest Package Permissions and Capabilities without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- None.

# Allowed Paths

- docs/dynaxis/program/**
- docs/dynaxis/*.md

# Forbidden Paths

- lib/**
- app/**
- packages/**
- drizzle/**
- runtime schemas
- production APIs
- product UI

# Required Deliverables

- architecture specification for Plugin Manifest Package Permissions, Capabilities
- domain model, contracts, invariants, ownership, failure behavior, and non-goals for Plugin Manifest Package Permissions, Capabilities
- dependency map identifying which runtime packages consume the specification
- Plugin Platform governs extension packages, permissions, signing, sandboxing, and lifecycle over canonical Dynaxis registries.
- Specification only: inspect runtime code as needed but edit docs/programme files only.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- The specification names the concrete objects, contracts, invariants, and API boundaries future implementers must follow.
- Allowed paths are documentation-only and migration_owner is false.

# Validation

- review checklist proving Plugin Manifest Package Permissions, Capabilities can be implemented without inventing architecture
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
