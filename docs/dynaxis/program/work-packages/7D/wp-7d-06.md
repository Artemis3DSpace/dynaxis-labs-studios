---
id: WP-7D-06
phase: 7D
title: Connection Health Rotation UI and Audit
status: review
agent: claude
base_sha: 15938784ad842a10822adff030f20bd4fe2ab725
branch: phase-7d/connection-health-rotation-ui-audit
worktree: dynaxis-labs-studios
depends_on: ["WP-7D-04", "WP-7D-05"]
blocks: ["WP-7D-07"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["lib/dynaxis/provider-connections/**", "lib/dynaxis/secrets/**", "lib/dynaxis/providers/**", "app/api/dynaxis/provider-connections/**", "packages/studio/src/**", "tests/dynaxis-provider-connections*.test.mjs"]
---

# Objective

Deliver Connection Health Rotation UI and Audit for Phase 7D. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Provider credentials are Dynaxis Provider Connections owned by users/workspaces; they never become identity or authorization subjects. This Work Package is intentionally narrow: it must deliver Connection Health Rotation UI and Audit without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7D-04
- WP-7D-05

# Allowed Paths

- lib/dynaxis/provider-connections/**
- lib/dynaxis/secrets/**
- lib/dynaxis/providers/**
- app/api/dynaxis/provider-connections/**
- packages/studio/src/**
- tests/dynaxis-provider-connections*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- UI/API for listing connection status, rotating credentials, disabling connections, and viewing safe audit metadata
- health-check flow that never exposes secret values to the client
- workspace-scoped UX states for missing, invalid, expiring, and healthy credentials
- Provider credentials are Dynaxis Provider Connections owned by users/workspaces; they never become identity or authorization subjects.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- UI/API for listing connection status, rotating credentials, disabling connections, and viewing safe audit metadata is present and documented/tested.
- health-check flow that never exposes secret values to the client is present and documented/tested.
- workspace-scoped UX states for missing, invalid, expiring, and healthy credentials is present and documented/tested.

# Validation

- targeted tests or review evidence for Connection Health Rotation UI and Audit
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
