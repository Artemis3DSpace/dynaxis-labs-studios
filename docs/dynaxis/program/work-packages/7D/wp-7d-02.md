---
id: WP-7D-02
phase: 7D
title: Secret Storage and Key Management Architecture
status: review
agent: claude
base_sha: 1803f70752825eec46da5a18db7a9967743054a6
branch: phase-7d/secret-storage-key-management-architecture
worktree: null
depends_on: ["WP-7D-01"]
blocks: ["WP-7D-03"]
migration_owner: false
type: specification
recommended_agent: claude
shared_files: []
---

# Objective

Deliver Secret Storage and Key Management Architecture for Phase 7D. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Provider credentials are Dynaxis Provider Connections owned by users/workspaces; they never become identity or authorization subjects. This Work Package is intentionally narrow: it must deliver Secret Storage and Key Management Architecture without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7D-01

# Allowed Paths

- docs/dynaxis/program/**
- docs/dynaxis/*.md

# Forbidden Paths

- lib/**
- app/**
- packages/**
- drizzle/**
- runtime schemas
- production APIs
- product UI

# Required Deliverables

- secret envelope contract separating encrypted payloads from ProviderConnection metadata
- key-management plan for local/dev, production KMS or equivalent, rotation, and audit evidence
- failure behavior for missing key, corrupted ciphertext, wrong workspace, and provider health checks
- Provider credentials are Dynaxis Provider Connections owned by users/workspaces; they never become identity or authorization subjects.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- secret envelope contract separating encrypted payloads from ProviderConnection metadata is present and documented/tested.
- key-management plan for local/dev, production KMS or equivalent, rotation, and audit evidence is present and documented/tested.
- failure behavior for missing key, corrupted ciphertext, wrong workspace, and provider health checks is present and documented/tested.

# Validation

- targeted tests or review evidence for Secret Storage and Key Management Architecture
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
