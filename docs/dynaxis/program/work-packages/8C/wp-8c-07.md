---
id: WP-8C-07
phase: 8C
title: Composer Domain Integration Tests and Evaluations
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8C-02", "WP-8C-03", "WP-8C-04", "WP-8C-05", "WP-8C-06"]
blocks: []
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/composer/**", "lib/dynaxis/compositions/**", "packages/mini-apps/creative-editor/**", "app/api/dynaxis/composer/**", "tests/dynaxis-composer*.test.mjs"]
---

# Objective

Deliver Composer Domain Integration Tests and Evaluations for Phase 8C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Composer is the timeline/media composition domain and render graph; it is not Auto Layout or App Factory. This Work Package is intentionally narrow: it must deliver Composer Domain Integration Tests and Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8C-02
- WP-8C-03
- WP-8C-04
- WP-8C-05
- WP-8C-06

# Allowed Paths

- lib/dynaxis/composer/**
- lib/dynaxis/compositions/**
- packages/mini-apps/creative-editor/**
- app/api/dynaxis/composer/**
- tests/dynaxis-composer*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes
- Auto Layout semantics
- App Factory Build runtime

# Required Deliverables

- integration tests across sequence persistence, editor mutations, media import, generative blocks, effects/audio/templates, and render workers
- evaluation fixtures for timeline correctness, render determinism, and Job Engine integration
- review evidence that Composer does not implement Auto Layout or App Factory responsibilities
- Composer is the timeline/media composition domain and render graph; it is not Auto Layout or App Factory. This Work Package is intentionally narrow: it must deliver Composer Domain Integration Tests and Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- integration tests across sequence persistence, editor mutations, media import, generative blocks, effects/audio/templates, and render workers is implemented, documented, or verified within this package.
- evaluation fixtures for timeline correctness, render determinism, and Job Engine integration is implemented, documented, or verified within this package.

# Validation

- performance/large timeline smoke coverage where practical
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
