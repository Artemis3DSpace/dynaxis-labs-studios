---
id: WP-8C-03
phase: 8C
title: Timeline Editor and Program Monitor UI
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8C-02"]
blocks: ["WP-8C-07"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["lib/dynaxis/composer/**", "lib/dynaxis/compositions/**", "packages/mini-apps/creative-editor/**", "app/api/dynaxis/composer/**", "tests/dynaxis-composer*.test.mjs"]
---

# Objective

Deliver Timeline Editor and Program Monitor UI for Phase 8C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Composer is the timeline/media composition domain and render graph; it is not Auto Layout or App Factory. This Work Package is intentionally narrow: it must deliver Timeline Editor and Program Monitor UI without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8C-02

# Allowed Paths

- lib/dynaxis/composer/**
- lib/dynaxis/compositions/**
- packages/mini-apps/creative-editor/**
- app/api/dynaxis/composer/**
- tests/dynaxis-composer*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Auto Layout semantics
- App Factory Build runtime

# Required Deliverables

- timeline editor UI for tracks, clips, markers, selection, trim/move operations, and playhead state
- program monitor view driven by canonical sequence/clip state rather than local-only UI state
- optimistic update and error rollback behavior for timeline mutations
- Composer is the timeline/media composition domain and render graph; it is not Auto Layout or App Factory. This Work Package is intentionally narrow: it must deliver Timeline Editor and Program Monitor UI without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- timeline editor UI for tracks, clips, markers, selection, trim/move operations, and playhead state is implemented, documented, or verified within this package.
- program monitor view driven by canonical sequence/clip state rather than local-only UI state is implemented, documented, or verified within this package.

# Validation

- visual/interaction tests for dense timelines and mobile/desktop constraints where applicable
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
