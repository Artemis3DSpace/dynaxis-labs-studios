---
id: WP-8C-05
phase: 8C
title: Effects Transitions Audio and Templates
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8C-02"]
blocks: ["WP-8C-06", "WP-8C-07"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/composer/**", "lib/dynaxis/compositions/**", "packages/mini-apps/creative-editor/**", "app/api/dynaxis/composer/**", "tests/dynaxis-composer*.test.mjs"]
---

# Objective

Deliver Effects Transitions Audio and Templates for Phase 8C. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Composer is the timeline/media composition domain and render graph; it is not Auto Layout or App Factory. This Work Package is intentionally narrow: it must deliver Effects Transitions Audio and Templates without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8C-02

# Allowed Paths

- lib/dynaxis/composer/**
- lib/dynaxis/compositions/**
- packages/mini-apps/creative-editor/**
- app/api/dynaxis/composer/**
- tests/dynaxis-composer*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Auto Layout semantics
- App Factory Build runtime

# Required Deliverables

- effect, transition, audio clip, audio mix, and template contracts within the Composer sequence domain
- validation for compatible media types, durations, parameter schema, and render ordering
- template instantiate behavior preserving Project, Asset, Character, Product, and Brand references
- Composer is the timeline/media composition domain and render graph; it is not Auto Layout or App Factory. This Work Package is intentionally narrow: it must deliver Effects Transitions Audio and Templates without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- effect, transition, audio clip, audio mix, and template contracts within the Composer sequence domain is implemented, documented, or verified within this package.
- validation for compatible media types, durations, parameter schema, and render ordering is implemented, documented, or verified within this package.

# Validation

- tests for incompatible effect/transition/audio parameters and template round trips
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
