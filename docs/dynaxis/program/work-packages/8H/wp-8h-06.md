---
id: WP-8H-06
phase: 8H
title: Creator Analytics Private Packages and Partner Tiers
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8H-04", "WP-8H-05"]
blocks: ["WP-8H-07"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["lib/dynaxis/marketplace/**", "lib/dynaxis/entitlements/**", "app/api/dynaxis/marketplace/**", "packages/studio/src/**", "tests/dynaxis-marketplace*.test.mjs"]
---

# Objective

Deliver Creator Analytics Private Packages and Partner Tiers for Phase 8H. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Marketplace is a distribution, trust, licensing, and installation layer over canonical registries, not duplicate registries. This Work Package is intentionally narrow: it must deliver Creator Analytics Private Packages and Partner Tiers without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8H-04
- WP-8H-05

# Allowed Paths

- lib/dynaxis/marketplace/**
- lib/dynaxis/entitlements/**
- app/api/dynaxis/marketplace/**
- packages/studio/src/**
- tests/dynaxis-marketplace*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- duplicate model/skill/component/plugin registries
- payment or payout processing before Phase 10

# Required Deliverables

- creator analytics events and aggregate views for installs, updates, usage signals, and conversion without payment/payout logic
- private package sharing and access-list behavior for selected workspaces/users
- partner tier flags for Dynaxis Official, Verified Partners, and Selected Creators
- Marketplace is a distribution, trust, licensing, and installation layer over canonical registries, not duplicate registries. This Work Package is intentionally narrow: it must deliver Creator Analytics Private Packages and Partner Tiers without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- creator analytics events and aggregate views for installs, updates, usage signals, and conversion without payment/payout logic is implemented, documented, or verified within this package.
- private package sharing and access-list behavior for selected workspaces/users is implemented, documented, or verified within this package.

# Validation

- tests for analytics scoping, private visibility, and partner badge rules
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
