---
id: WP-8H-04
phase: 8H
title: Licensing Entitlements Collections and Profiles
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8H-03"]
blocks: ["WP-8H-05", "WP-8H-06", "WP-8H-07"]
migration_owner: true
type: implementation
recommended_agent: cursor
shared_files: ["lib/dynaxis/marketplace/**", "lib/dynaxis/entitlements/**", "app/api/dynaxis/marketplace/**", "packages/studio/src/**", "tests/dynaxis-marketplace*.test.mjs"]
---

# Objective

Deliver Licensing Entitlements Collections and Profiles for Phase 8H. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Marketplace is a distribution, trust, licensing, and installation layer over canonical registries, not duplicate registries. This Work Package is intentionally narrow: it must deliver Licensing Entitlements Collections and Profiles without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8H-03

# Allowed Paths

- lib/dynaxis/marketplace/**
- lib/dynaxis/entitlements/**
- app/api/dynaxis/marketplace/**
- packages/studio/src/**
- tests/dynaxis-marketplace*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- duplicate model/skill/component/plugin registries
- payment or payout processing before Phase 10

# Required Deliverables

- licensing and entitlement records for package access without implementing payment processing
- collection and creator-profile model tied to publisher identity and package visibility
- authorization hooks for install/use based on workspace entitlement and package license
- Marketplace is a distribution, trust, licensing, and installation layer over canonical registries, not duplicate registries. This Work Package is intentionally narrow: it must deliver Licensing Entitlements Collections and Profiles without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- licensing and entitlement records for package access without implementing payment processing is implemented, documented, or verified within this package.
- collection and creator-profile model tied to publisher identity and package visibility is implemented, documented, or verified within this package.

# Validation

- tests for entitlement denial, private package visibility, and collection membership
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
