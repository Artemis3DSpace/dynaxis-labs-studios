---
id: WP-8H-03
phase: 8H
title: Versions Installation Updates and Uninstall
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8H-02"]
blocks: ["WP-8H-04", "WP-8H-07"]
migration_owner: true
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/marketplace/**", "lib/dynaxis/entitlements/**", "app/api/dynaxis/marketplace/**", "packages/studio/src/**", "tests/dynaxis-marketplace*.test.mjs"]
---

# Objective

Deliver Versions Installation Updates and Uninstall for Phase 8H. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Marketplace is a distribution, trust, licensing, and installation layer over canonical registries, not duplicate registries. This Work Package is intentionally narrow: it must deliver Versions Installation Updates and Uninstall without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8H-02

# Allowed Paths

- lib/dynaxis/marketplace/**
- lib/dynaxis/entitlements/**
- app/api/dynaxis/marketplace/**
- packages/studio/src/**
- tests/dynaxis-marketplace*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- duplicate model/skill/component/plugin registries
- payment or payout processing before Phase 10

# Required Deliverables

- package version, install record, update channel, uninstall tombstone, and compatibility state persistence
- installer service that delegates to canonical registry/plugin/skill/component installers and records provenance
- failure behavior for incompatible version, missing entitlement, partial install, and uninstall rollback
- Marketplace is a distribution, trust, licensing, and installation layer over canonical registries, not duplicate registries. This Work Package is intentionally narrow: it must deliver Versions Installation Updates and Uninstall without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- package version, install record, update channel, uninstall tombstone, and compatibility state persistence is implemented, documented, or verified within this package.
- installer service that delegates to canonical registry/plugin/skill/component installers and records provenance is implemented, documented, or verified within this package.

# Validation

- tests for install/update/uninstall lifecycle and idempotent retries
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
