---
id: WP-7E-04
phase: 7E
title: Job Schema Migration and Persistence
status: review
agent: codex
base_sha: 012c2c30eda554733f181a2be7b76eb002197e5d
branch: phase-7e/job-schema-persistence
worktree: /Users/sotiratrifourki/Documents/dynaxis-labs-studios-July-26/phase-7e-job-schema-persistence-private
depends_on: ["WP-7D-07", "WP-7E-01", "WP-7E-03"]
blocks: ["WP-7E-05", "WP-7E-10"]
migration_owner: true
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/jobs/**", "lib/dynaxis/events/**", "lib/dynaxis/queues/**", "lib/dynaxis/workers/**", "app/api/dynaxis/jobs/**", "app/api/dynaxis/webhooks/**", "tests/dynaxis-job-engine*.test.mjs"]
---

# Objective

Deliver Job Schema Migration and Persistence for Phase 7E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration. This Work Package is intentionally narrow: it must deliver Job Schema Migration and Persistence without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7D-07
- WP-7E-01
- WP-7E-03

# Allowed Paths

- lib/dynaxis/jobs/**
- lib/dynaxis/events/**
- lib/dynaxis/queues/**
- lib/dynaxis/workers/**
- app/api/dynaxis/jobs/**
- app/api/dynaxis/webhooks/**
- tests/dynaxis-job-engine*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- durable job execution tables for attempts, events, leases, idempotency keys, and audit timeline
- indexes for polling, worker leasing, provider callback lookup, and project history
- migration tests for transition constraints and no destructive rewrite of existing jobs
- The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration.

## Implementation Status (current branch)

- Added migration `0016_phase_7e_4_job_persistence.sql` with durable
  `dynaxis_job_records` and `dynaxis_job_events` tables plus journal entry.
- Added persistence-only jobs modules under `lib/dynaxis/jobs/`:
  `schema.js`, `persistence.js`, `schema-mapping.js`, `store.js`.
- Added persistence tests:
  `tests/dynaxis-jobs-persistence.test.mjs` and
  `tests/dynaxis-jobs-events-persistence.test.mjs`.
- Decision **D1** preserved: `timed_out` remains non-terminal in schema and tests.
- Decision **D2** preserved: no `cancelling` state added; requested-vs-confirmed
  cancellation remains metadata/event driven.
- Scope remains persistence-only: no queue dispatcher, no worker runtime, no
  provider adapter implementation, no OAuth.
- Phase 7D residual risk **R3** remains separate and unclosed: this package
  persists the Phase 7E job-event timeline, not the provider-connection audit sink.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- durable job execution tables for attempts, events, leases, idempotency keys, and audit timeline is present and documented/tested.
- indexes for polling, worker leasing, provider callback lookup, and project history is present and documented/tested.
- migration tests for transition constraints and no destructive rewrite of existing jobs is present and documented/tested.

# Validation

- targeted tests or review evidence for Job Schema Migration and Persistence
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
