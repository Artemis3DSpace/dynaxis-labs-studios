---
id: WP-7E-06
phase: 7E
title: Worker Runtime and Provider Worker Adapter
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7D-05", "WP-7E-05"]
blocks: ["WP-7E-07", "WP-7E-08", "WP-7E-10"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/jobs/**", "lib/dynaxis/events/**", "lib/dynaxis/queues/**", "lib/dynaxis/workers/**", "app/api/dynaxis/jobs/**", "app/api/dynaxis/webhooks/**", "tests/dynaxis-job-engine*.test.mjs"]
---

# Objective

Deliver Worker Runtime and Provider Worker Adapter for Phase 7E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration. This Work Package is intentionally narrow: it must deliver Worker Runtime and Provider Worker Adapter without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7D-05
- WP-7E-05

# Allowed Paths

- lib/dynaxis/jobs/**
- lib/dynaxis/events/**
- lib/dynaxis/queues/**
- lib/dynaxis/workers/**
- app/api/dynaxis/jobs/**
- app/api/dynaxis/webhooks/**
- tests/dynaxis-job-engine*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- worker runtime contract for execution lifecycle, heartbeat, cancellation, and artifact reporting
- provider worker adapter that calls Generation Provider adapters using ProviderConnection credentials
- safe handoff from Job Engine to provider without client-primary polling authority
- The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- worker runtime contract for execution lifecycle, heartbeat, cancellation, and artifact reporting is present and documented/tested.
- provider worker adapter that calls Generation Provider adapters using ProviderConnection credentials is present and documented/tested.
- safe handoff from Job Engine to provider without client-primary polling authority is present and documented/tested.

# Validation

- targeted tests or review evidence for Worker Runtime and Provider Worker Adapter
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
