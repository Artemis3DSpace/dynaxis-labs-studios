---
id: WP-7E-10
phase: 7E
title: Job Engine Load Failure and Integration Tests
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7E-04", "WP-7E-05", "WP-7E-06", "WP-7E-07", "WP-7E-08", "WP-7E-09"]
blocks: ["WP-7F-02", "WP-7I-02", "WP-8B-05", "WP-8C-02", "WP-8C-06", "WP-8E-02", "WP-8F-03", "WP-9-04", "WP-10-03"]
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/jobs/**", "lib/dynaxis/events/**", "lib/dynaxis/queues/**", "lib/dynaxis/workers/**", "app/api/dynaxis/jobs/**", "app/api/dynaxis/webhooks/**", "tests/dynaxis-job-engine*.test.mjs"]
---

# Objective

Deliver Job Engine Load Failure and Integration Tests for Phase 7E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration. This Work Package is intentionally narrow: it must deliver Job Engine Load Failure and Integration Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7E-04
- WP-7E-05
- WP-7E-06
- WP-7E-07
- WP-7E-08
- WP-7E-09

# Allowed Paths

- lib/dynaxis/jobs/**
- lib/dynaxis/events/**
- lib/dynaxis/queues/**
- lib/dynaxis/workers/**
- app/api/dynaxis/jobs/**
- app/api/dynaxis/webhooks/**
- tests/dynaxis-job-engine*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- integration tests covering state transitions, queue dispatch, webhook completion, retry, timeout, cancel, and reconciliation
- load/failure tests for duplicate events, worker crash, provider outage, and idempotent replays
- evidence that Job Engine remains execution authority
- The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- integration tests covering state transitions, queue dispatch, webhook completion, retry, timeout, cancel, and reconciliation is present and documented/tested.
- load/failure tests for duplicate events, worker crash, provider outage, and idempotent replays is present and documented/tested.
- evidence that Job Engine remains execution authority is present and documented/tested.

# Validation

- targeted tests or review evidence for Job Engine Load Failure and Integration Tests
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
