---
id: WP-7E-03
phase: 7E
title: Job Event Model and Audit Timeline
status: review
agent: claude
base_sha: b0b5555ab6a8b46100d19b19b58318220b3ec7b8
branch: phase-7e/job-state-event-specs
worktree: /Users/sotiratrifourki/Documents/dynaxis-labs-studios-July-26/phase-7e-specs-private
depends_on: ["WP-7E-01"]
blocks: ["WP-7E-04", "WP-7E-07"]
migration_owner: false
type: specification
recommended_agent: codex
shared_files: []
---

# Objective

Deliver Job Event Model and Audit Timeline for Phase 7E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration. This Work Package is intentionally narrow: it must deliver Job Event Model and Audit Timeline without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- None.

# Allowed Paths

- docs/dynaxis/program/**
- docs/dynaxis/*.md

# Forbidden Paths

- lib/**
- app/**
- packages/**
- drizzle/**
- runtime schemas
- production APIs
- product UI

# Required Deliverables

- event schema for job created, dispatched, provider-updated, retried, completed, failed, cancelled, and reconciled
- audit timeline projection rules and retention expectations
- correlation ids for Generation, Asset, ProviderConnection, worker, and request provenance
- The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration.

Delivered in `docs/dynaxis/PHASE_7E_JOB_EVENT_MODEL.md`, reconciled against the `SD-01` jobs scaffold already on `main`. `JOB_EVENT_NAMES`, `REDACTED_EVENT_PAYLOAD_KEYS`, and `CORRELATION_ID_FIELDS` in `lib/dynaxis/jobs/contracts.js` are canonical. §8 records that the Job event timeline is a **different boundary** from the Phase 7D provider-connection audit sink: persisting job events does **not** resolve residual risk **R3**, which remains unowned.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- event schema for job created, dispatched, provider-updated, retried, completed, failed, cancelled, and reconciled is present and documented/tested.
- audit timeline projection rules and retention expectations is present and documented/tested.
- correlation ids for Generation, Asset, ProviderConnection, worker, and request provenance is present and documented/tested.

# Validation

- targeted tests or review evidence for Job Event Model and Audit Timeline
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
