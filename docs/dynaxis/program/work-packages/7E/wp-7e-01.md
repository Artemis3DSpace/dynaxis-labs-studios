---
id: WP-7E-01
phase: 7E
title: Job State Machine Specification
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: []
blocks: ["WP-7E-04", "WP-8E-01"]
migration_owner: false
type: specification
recommended_agent: claude
shared_files: []
---

# Objective

Deliver Job State Machine Specification for Phase 7E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration. This Work Package is intentionally narrow: it must deliver Job State Machine Specification without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- None.

# Allowed Paths

- docs/dynaxis/program/**
- docs/dynaxis/*.md

# Forbidden Paths

- lib/**
- app/**
- packages/**
- drizzle/**
- runtime schemas
- production APIs
- product UI

# Required Deliverables

- canonical job states, terminal states, allowed transitions, and transition ownership
- provider-job metadata rules and distinction between Dynaxis Job id and provider job id
- failure, cancellation, timeout, retry, and reconciliation semantics
- The Job Engine is the durable execution authority upstream of provider adapters, webhooks, retries, and asset registration.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- canonical job states, terminal states, allowed transitions, and transition ownership is present and documented/tested.
- provider-job metadata rules and distinction between Dynaxis Job id and provider job id is present and documented/tested.
- failure, cancellation, timeout, retry, and reconciliation semantics is present and documented/tested.

# Validation

- targeted tests or review evidence for Job State Machine Specification
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
