---
id: WP-8D-02
phase: 8D
title: Stack Constraints Padding Gaps and Sizing Rules
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8A-07", "WP-8D-01"]
blocks: ["WP-8D-03", "WP-8D-04", "WP-8D-06"]
migration_owner: true
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/layout/**", "lib/dynaxis/app-ir/**", "lib/dynaxis/design-systems/**", "lib/dynaxis/components/**", "packages/mini-apps/design-library/**", "packages/mini-apps/creative-editor/**", "tests/dynaxis-layout*.test.mjs"]
---

# Objective

Deliver Stack Constraints Padding Gaps and Sizing Rules for Phase 8D. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Responsive Design defines Layout IR and visual application design semantics that bridge Design Systems and App IR. This Work Package is intentionally narrow: it must deliver Stack Constraints Padding Gaps and Sizing Rules without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8A-07
- WP-8D-01

# Allowed Paths

- lib/dynaxis/layout/**
- lib/dynaxis/app-ir/**
- lib/dynaxis/design-systems/**
- lib/dynaxis/components/**
- packages/mini-apps/design-library/**
- packages/mini-apps/creative-editor/**
- tests/dynaxis-layout*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Composer timeline/render graph semantics

# Required Deliverables

- Layout IR primitives for stack direction, constraints, padding, gaps, alignment, sizing, min/max, and overflow behavior
- validation service for conflicting constraints and invalid responsive values
- persistence/migration ownership for core layout primitives where required
- Responsive Design defines Layout IR and visual application design semantics that bridge Design Systems and App IR. This Work Package is intentionally narrow: it must deliver Stack Constraints Padding Gaps and Sizing Rules without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Layout IR primitives for stack direction, constraints, padding, gaps, alignment, sizing, min/max, and overflow behavior is implemented, documented, or verified within this package.
- validation service for conflicting constraints and invalid responsive values is implemented, documented, or verified within this package.

# Validation

- tests for constraint solving edge cases and invalid layout documents
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
