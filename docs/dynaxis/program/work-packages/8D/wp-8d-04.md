---
id: WP-8D-04
phase: 8D
title: Visual Inspector and Design Canvas UI
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8D-02", "WP-8D-03"]
blocks: ["WP-8D-06"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["lib/dynaxis/layout/**", "lib/dynaxis/app-ir/**", "lib/dynaxis/design-systems/**", "lib/dynaxis/components/**", "packages/mini-apps/design-library/**", "packages/mini-apps/creative-editor/**", "tests/dynaxis-layout*.test.mjs"]
---

# Objective

Deliver Visual Inspector and Design Canvas UI for Phase 8D. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Responsive Design defines Layout IR and visual application design semantics that bridge Design Systems and App IR. This Work Package is intentionally narrow: it must deliver Visual Inspector and Design Canvas UI without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8D-02
- WP-8D-03

# Allowed Paths

- lib/dynaxis/layout/**
- lib/dynaxis/app-ir/**
- lib/dynaxis/design-systems/**
- lib/dynaxis/components/**
- packages/mini-apps/design-library/**
- packages/mini-apps/creative-editor/**
- tests/dynaxis-layout*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions
- Composer timeline/render graph semantics

# Required Deliverables

- visual inspector UI for selecting elements and editing layout properties through structured controls
- design canvas interaction model for responsive previews, constraint feedback, and layout state visualization
- error display/rollback behavior for invalid edits and failed saves
- Responsive Design defines Layout IR and visual application design semantics that bridge Design Systems and App IR. This Work Package is intentionally narrow: it must deliver Visual Inspector and Design Canvas UI without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- visual inspector UI for selecting elements and editing layout properties through structured controls is implemented, documented, or verified within this package.
- design canvas interaction model for responsive previews, constraint feedback, and layout state visualization is implemented, documented, or verified within this package.

# Validation

- Playwright/visual checks that controls do not overlap and text fits in inspector surfaces
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
