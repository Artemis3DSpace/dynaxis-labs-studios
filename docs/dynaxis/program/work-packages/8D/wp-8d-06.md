---
id: WP-8D-06
phase: 8D
title: Responsive Design Tests and Visual Evaluations
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8D-02", "WP-8D-03", "WP-8D-04", "WP-8D-05"]
blocks: []
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/layout/**", "lib/dynaxis/app-ir/**", "lib/dynaxis/design-systems/**", "lib/dynaxis/components/**", "packages/mini-apps/design-library/**", "packages/mini-apps/creative-editor/**", "tests/dynaxis-layout*.test.mjs"]
---

# Objective

Deliver Responsive Design Tests and Visual Evaluations for Phase 8D. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Responsive Design defines Layout IR and visual application design semantics that bridge Design Systems and App IR. This Work Package is intentionally narrow: it must deliver Responsive Design Tests and Visual Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8D-02
- WP-8D-03
- WP-8D-04
- WP-8D-05

# Allowed Paths

- lib/dynaxis/layout/**
- lib/dynaxis/app-ir/**
- lib/dynaxis/design-systems/**
- lib/dynaxis/components/**
- packages/mini-apps/design-library/**
- packages/mini-apps/creative-editor/**
- tests/dynaxis-layout*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes
- Composer timeline/render graph semantics

# Required Deliverables

- test suite for Layout IR validation, breakpoint behavior, visual inspector interactions, App IR bridge, and code-generation hints
- visual evaluation matrix across desktop/mobile viewports and dense UI states
- review report proving Auto Layout stays separate from Composer timeline semantics
- Responsive Design defines Layout IR and visual application design semantics that bridge Design Systems and App IR. This Work Package is intentionally narrow: it must deliver Responsive Design Tests and Visual Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- test suite for Layout IR validation, breakpoint behavior, visual inspector interactions, App IR bridge, and code-generation hints is implemented, documented, or verified within this package.
- visual evaluation matrix across desktop/mobile viewports and dense UI states is implemented, documented, or verified within this package.

# Validation

- residual gaps before production code-generation consumers use Layout IR
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
