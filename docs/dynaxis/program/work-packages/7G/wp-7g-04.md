---
id: WP-7G-04
phase: 7G
title: Capability Resolver Routing and Entitlement Hooks
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7D-05", "WP-7G-03"]
blocks: ["WP-7G-05"]
migration_owner: false
type: implementation
recommended_agent: claude
shared_files: ["lib/dynaxis/capabilities/**", "lib/dynaxis/model-registry/**", "lib/dynaxis/providers/**", "app/api/dynaxis/capabilities/**", "packages/studio/src/models.js", "tests/dynaxis-capability*.test.mjs"]
---

# Objective

Deliver Capability Resolver Routing and Entitlement Hooks for Phase 7G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Capability and model metadata becomes a governed Dynaxis registry seeded from current studio models and provider-kernel contracts. This Work Package is intentionally narrow: it must deliver Capability Resolver Routing and Entitlement Hooks without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7D-05
- WP-7G-03

# Allowed Paths

- lib/dynaxis/capabilities/**
- lib/dynaxis/model-registry/**
- lib/dynaxis/providers/**
- app/api/dynaxis/capabilities/**
- packages/studio/src/models.js
- tests/dynaxis-capability*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- resolver inputs for requested capability, model constraints, provider connection availability, workspace entitlements, and policy context
- routing result contract explaining selected provider/model/version and alternatives
- failure behavior for no capable model, missing connection, disabled provider, and entitlement denial
- Capability and model metadata becomes a governed Dynaxis registry seeded from current studio models and provider-kernel contracts. This Work Package is intentionally narrow: it must deliver Capability Resolver Routing and Entitlement Hooks without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- resolver inputs for requested capability, model constraints, provider connection availability, workspace entitlements, and policy context is implemented, documented, or verified within this package.
- routing result contract explaining selected provider/model/version and alternatives is implemented, documented, or verified within this package.

# Validation

- tests for provider-neutral routing and denied entitlement hooks
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
