---
id: WP-7G-03
phase: 7G
title: Cost Latency Quality and Availability Metadata
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7G-02"]
blocks: ["WP-7G-04", "WP-7G-05"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/capabilities/**", "lib/dynaxis/model-registry/**", "lib/dynaxis/providers/**", "app/api/dynaxis/capabilities/**", "packages/studio/src/models.js", "tests/dynaxis-capability*.test.mjs"]
---

# Objective

Deliver Cost Latency Quality and Availability Metadata for Phase 7G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Capability and model metadata becomes a governed Dynaxis registry seeded from current studio models and provider-kernel contracts. This Work Package is intentionally narrow: it must deliver Cost Latency Quality and Availability Metadata without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7G-02

# Allowed Paths

- lib/dynaxis/capabilities/**
- lib/dynaxis/model-registry/**
- lib/dynaxis/providers/**
- app/api/dynaxis/capabilities/**
- packages/studio/src/models.js
- tests/dynaxis-capability*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- metadata model for cost units, latency bands, quality labels, availability state, and regional/provider constraints
- service methods for reading/updating registry metadata with audit provenance
- fallback behavior when metadata is unknown, stale, or provider-specific
- Capability and model metadata becomes a governed Dynaxis registry seeded from current studio models and provider-kernel contracts. This Work Package is intentionally narrow: it must deliver Cost Latency Quality and Availability Metadata without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- metadata model for cost units, latency bands, quality labels, availability state, and regional/provider constraints is implemented, documented, or verified within this package.
- service methods for reading/updating registry metadata with audit provenance is implemented, documented, or verified within this package.

# Validation

- tests for resolver-visible metadata and invalid unit/value rejection
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
