---
id: WP-7G-05
phase: 7G
title: Registry Administration and Tests
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7G-02", "WP-7G-03", "WP-7G-04"]
blocks: ["WP-7H-02", "WP-8A-04", "WP-8C-02", "WP-8E-02"]
migration_owner: false
type: implementation
recommended_agent: cursor
shared_files: ["lib/dynaxis/capabilities/**", "lib/dynaxis/model-registry/**", "lib/dynaxis/providers/**", "app/api/dynaxis/capabilities/**", "packages/studio/src/models.js", "tests/dynaxis-capability*.test.mjs"]
---

# Objective

Deliver Registry Administration and Tests for Phase 7G. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Capability and model metadata becomes a governed Dynaxis registry seeded from current studio models and provider-kernel contracts. This Work Package is intentionally narrow: it must deliver Registry Administration and Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7G-02
- WP-7G-03
- WP-7G-04

# Allowed Paths

- lib/dynaxis/capabilities/**
- lib/dynaxis/model-registry/**
- lib/dynaxis/providers/**
- app/api/dynaxis/capabilities/**
- packages/studio/src/models.js
- tests/dynaxis-capability*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- admin API/UI surface for listing, editing, disabling, and auditing capability/model registry records
- safe Studio-facing read path that cannot mutate registry state
- test suite covering admin validation, availability changes, and resolver integration
- Capability and model metadata becomes a governed Dynaxis registry seeded from current studio models and provider-kernel contracts. This Work Package is intentionally narrow: it must deliver Registry Administration and Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- admin API/UI surface for listing, editing, disabling, and auditing capability/model registry records is implemented, documented, or verified within this package.
- safe Studio-facing read path that cannot mutate registry state is implemented, documented, or verified within this package.

# Validation

- no Marketplace package catalogue or duplicate registry persistence
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
