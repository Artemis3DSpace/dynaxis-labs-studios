---
id: WP-8E-05
phase: 8E
title: Agent Integration and Skill Runtime Tests
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8E-02", "WP-8E-03", "WP-8E-04"]
blocks: ["WP-8G-02", "WP-9-02", "WP-9-03"]
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/skills/**", "lib/dynaxis/agents/**", "lib/dynaxis/jobs/**", "app/api/dynaxis/skills/**", "tests/dynaxis-skills*.test.mjs"]
---

# Objective

Deliver Agent Integration and Skill Runtime Tests for Phase 8E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Skills are permissioned reusable capability packages callable by workflows and agents through Jobs and contracts. This Work Package is intentionally narrow: it must deliver Agent Integration and Skill Runtime Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8E-02
- WP-8E-03
- WP-8E-04

# Allowed Paths

- lib/dynaxis/skills/**
- lib/dynaxis/agents/**
- lib/dynaxis/jobs/**
- app/api/dynaxis/skills/**
- tests/dynaxis-skills*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- agent-to-skill invocation contract using AgentRole permissions and WorkPackage scope
- runtime tests covering skill discovery, permission projection, invocation, output validation, and audit trail
- negative tests for unauthorized agent skill use and stale dependency versions
- Skills are permissioned reusable capability packages callable by workflows and agents through Jobs and contracts. This Work Package is intentionally narrow: it must deliver Agent Integration and Skill Runtime Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- agent-to-skill invocation contract using AgentRole permissions and WorkPackage scope is implemented, documented, or verified within this package.
- runtime tests covering skill discovery, permission projection, invocation, output validation, and audit trail is implemented, documented, or verified within this package.

# Validation

- review evidence before Supercomputer consumes Skills
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
