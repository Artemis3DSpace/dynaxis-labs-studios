---
id: WP-8E-02
phase: 8E
title: Skill Permissions Invocation IO and Versioning
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-23", "WP-7E-10", "WP-7G-05", "WP-7I-05", "WP-8E-01"]
blocks: ["WP-8E-03", "WP-8E-04", "WP-8E-05"]
migration_owner: true
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/skills/**", "lib/dynaxis/agents/**", "lib/dynaxis/jobs/**", "app/api/dynaxis/skills/**", "tests/dynaxis-skills*.test.mjs"]
---

# Objective

Deliver Skill Permissions Invocation IO and Versioning for Phase 8E. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Skills are permissioned reusable capability packages callable by workflows and agents through Jobs and contracts. This Work Package is intentionally narrow: it must deliver Skill Permissions Invocation IO and Versioning without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-23
- WP-7E-10
- WP-7G-05
- WP-7I-05
- WP-8E-01

# Allowed Paths

- lib/dynaxis/skills/**
- lib/dynaxis/agents/**
- lib/dynaxis/jobs/**
- app/api/dynaxis/skills/**
- tests/dynaxis-skills*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- Skill registry persistence for manifests, versions, permissions, inputs, outputs, execution policy, and provenance
- invocation runtime contract that validates inputs/outputs and dispatches through Jobs where execution is durable
- version compatibility and deprecation behavior for callers
- Skills are permissioned reusable capability packages callable by workflows and agents through Jobs and contracts. This Work Package is intentionally narrow: it must deliver Skill Permissions Invocation IO and Versioning without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Skill registry persistence for manifests, versions, permissions, inputs, outputs, execution policy, and provenance is implemented, documented, or verified within this package.
- invocation runtime contract that validates inputs/outputs and dispatches through Jobs where execution is durable is implemented, documented, or verified within this package.

# Validation

- tests for permission denial, invalid IO, version selection, and job-backed invocation
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
