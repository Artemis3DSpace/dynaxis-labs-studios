---
id: WP-7I-03
phase: 7I
title: WorkerAdapter and Capability Contract
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7I-02"]
blocks: ["WP-7I-05"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/agents/**", "lib/dynaxis/engineering/**", "lib/dynaxis/verification/**", "tests/dynaxis-agent-contracts*.test.mjs"]
---

# Objective

Deliver WorkerAdapter and Capability Contract for Phase 7I. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver WorkerAdapter and Capability Contract without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7I-02

# Allowed Paths

- lib/dynaxis/agents/**
- lib/dynaxis/engineering/**
- lib/dynaxis/verification/**
- tests/dynaxis-agent-contracts*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- WorkerAdapter interface for provider id, capabilities, input envelope, progress, artifacts, cancellation, and timeout
- capability matching rules between AgentRole, WorkPackage requirements, and worker provider abilities
- structured execution errors for unsupported capability, missing credential, timeout, and cancelled run
- Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver WorkerAdapter and Capability Contract without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- WorkerAdapter interface for provider id, capabilities, input envelope, progress, artifacts, cancellation, and timeout is implemented, documented, or verified within this package.
- capability matching rules between AgentRole, WorkPackage requirements, and worker provider abilities is implemented, documented, or verified within this package.

# Validation

- tests with mock Codex/Claude/Cursor adapters without implementing provider integrations
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
