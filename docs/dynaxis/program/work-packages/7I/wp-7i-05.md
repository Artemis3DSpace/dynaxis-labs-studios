---
id: WP-7I-05
phase: 7I
title: Agent Contract Fixtures Audit and Provenance Tests
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7I-02", "WP-7I-03", "WP-7I-04"]
blocks: ["WP-8A-02", "WP-8A-05", "WP-8B-03", "WP-8B-05", "WP-8E-02", "WP-9-03"]
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/agents/**", "lib/dynaxis/engineering/**", "lib/dynaxis/verification/**", "tests/dynaxis-agent-contracts*.test.mjs"]
---

# Objective

Deliver Agent Contract Fixtures Audit and Provenance Tests for Phase 7I. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver Agent Contract Fixtures Audit and Provenance Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7I-02
- WP-7I-03
- WP-7I-04

# Allowed Paths

- lib/dynaxis/agents/**
- lib/dynaxis/engineering/**
- lib/dynaxis/verification/**
- tests/dynaxis-agent-contracts*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- fixtures covering AgentRole, WorkPackage, WorkerAdapter, VerificationGate, and ExecutionResult contracts
- audit trail tests proving every state transition records actor, source package, timestamp, and project context
- negative tests for unauthorized worker assignment and unverifiable completion
- Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver Agent Contract Fixtures Audit and Provenance Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- fixtures covering AgentRole, WorkPackage, WorkerAdapter, VerificationGate, and ExecutionResult contracts is implemented, documented, or verified within this package.
- audit trail tests proving every state transition records actor, source package, timestamp, and project context is implemented, documented, or verified within this package.

# Validation

- review report listing contract gaps before App Factory begins
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
