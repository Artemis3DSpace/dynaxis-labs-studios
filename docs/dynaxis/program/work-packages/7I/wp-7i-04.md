---
id: WP-7I-04
phase: 7I
title: VerificationGate and Execution Result Contract
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7I-02"]
blocks: ["WP-7I-05"]
migration_owner: false
type: implementation
recommended_agent: claude
shared_files: ["lib/dynaxis/agents/**", "lib/dynaxis/engineering/**", "lib/dynaxis/verification/**", "tests/dynaxis-agent-contracts*.test.mjs"]
---

# Objective

Deliver VerificationGate and Execution Result Contract for Phase 7I. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver VerificationGate and Execution Result Contract without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7I-02

# Allowed Paths

- lib/dynaxis/agents/**
- lib/dynaxis/engineering/**
- lib/dynaxis/verification/**
- tests/dynaxis-agent-contracts*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- VerificationGate contract for required evidence, automated/human mode, blocking severity, artifacts, and result status
- execution-result envelope for files changed, commands run, tests, screenshots/logs, and provenance references
- gate aggregation behavior for pass/fail/warn and software reliability impact
- Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver VerificationGate and Execution Result Contract without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- VerificationGate contract for required evidence, automated/human mode, blocking severity, artifacts, and result status is implemented, documented, or verified within this package.
- execution-result envelope for files changed, commands run, tests, screenshots/logs, and provenance references is implemented, documented, or verified within this package.

# Validation

- tests for blocking gate failure, missing evidence, and human-review-required state
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
