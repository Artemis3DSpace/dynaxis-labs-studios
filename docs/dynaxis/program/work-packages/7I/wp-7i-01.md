---
id: WP-7I-01
phase: 7I
title: Agent Role and Permission Contract Specification
status: ready
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: []
blocks: ["WP-7I-02", "WP-8E-01"]
migration_owner: false
type: specification
recommended_agent: claude
shared_files: []
---

# Objective

Deliver Agent Role and Permission Contract Specification for Phase 7I. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers. This Work Package is intentionally narrow: it must deliver Agent Role and Permission Contract Specification without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- None.

# Allowed Paths

- docs/dynaxis/program/**
- docs/dynaxis/*.md

# Forbidden Paths

- lib/**
- app/**
- packages/**
- drizzle/**
- runtime schemas
- production APIs
- product UI

# Required Deliverables

- architecture specification for Agent Role, Permission Contract Specification
- domain model, contracts, invariants, ownership, failure behavior, and non-goals for Agent Role, Permission Contract Specification
- dependency map identifying which runtime packages consume the specification
- Agent and engineering contracts define safe delegation boundaries; they do not implement Supercomputer, Skills runtime, or provider workers.
- Specification only: inspect runtime code as needed but edit docs/programme files only.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- The specification names the concrete objects, contracts, invariants, and API boundaries future implementers must follow.
- Allowed paths are documentation-only and migration_owner is false.

# Validation

- review checklist proving Agent Role, Permission Contract Specification can be implemented without inventing architecture
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
