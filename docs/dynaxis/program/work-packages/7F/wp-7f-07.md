---
id: WP-7F-07
phase: 7F
title: Graph and Memory Evaluations
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7F-02", "WP-7F-03", "WP-7F-04", "WP-7F-05", "WP-7F-06"]
blocks: ["WP-7G-02", "WP-7I-02", "WP-8A-02", "WP-8C-02", "WP-9-02"]
migration_owner: false
type: review
recommended_agent: claude
shared_files: ["lib/dynaxis/project-graph/**", "lib/dynaxis/memory/**", "lib/dynaxis/knowledge/**", "app/api/dynaxis/projects/**", "tests/dynaxis-project-graph*.test.mjs", "tests/dynaxis-memory*.test.mjs"]
---

# Objective

Deliver Graph and Memory Evaluations for Phase 7F. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Project Graph extends existing domain tables with typed edges, knowledge, memory, and decision provenance; it must not replace canonical domain persistence. This Work Package is intentionally narrow: it must deliver Graph and Memory Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7F-02
- WP-7F-03
- WP-7F-04
- WP-7F-05
- WP-7F-06

# Allowed Paths

- lib/dynaxis/project-graph/**
- lib/dynaxis/memory/**
- lib/dynaxis/knowledge/**
- app/api/dynaxis/projects/**
- tests/dynaxis-project-graph*.test.mjs
- tests/dynaxis-memory*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- evaluation corpus for graph traversal, memory grounding, retrieval relevance, and permission scoping
- review checklist proving existing domain tables remain canonical
- load/scale probes for common Project Graph query shapes
- Project Graph extends existing domain tables with typed edges, knowledge, memory, and decision provenance; it must not replace canonical domain persistence. This Work Package is intentionally narrow: it must deliver Graph and Memory Evaluations without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- evaluation corpus for graph traversal, memory grounding, retrieval relevance, and permission scoping is implemented, documented, or verified within this package.
- review checklist proving existing domain tables remain canonical is implemented, documented, or verified within this package.

# Validation

- documented residual risk and required fixes before 7G/7I consumers proceed
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
