---
id: WP-7F-03
phase: 7F
title: Memory Knowledge and Decision Records
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7F-02"]
blocks: ["WP-7F-04", "WP-7F-06", "WP-7F-07"]
migration_owner: true
type: implementation
recommended_agent: claude
shared_files: ["lib/dynaxis/project-graph/**", "lib/dynaxis/memory/**", "lib/dynaxis/knowledge/**", "app/api/dynaxis/projects/**", "tests/dynaxis-project-graph*.test.mjs", "tests/dynaxis-memory*.test.mjs"]
---

# Objective

Deliver Memory Knowledge and Decision Records for Phase 7F. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Project Graph extends existing domain tables with typed edges, knowledge, memory, and decision provenance; it must not replace canonical domain persistence. This Work Package is intentionally narrow: it must deliver Memory Knowledge and Decision Records without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7F-02

# Allowed Paths

- lib/dynaxis/project-graph/**
- lib/dynaxis/memory/**
- lib/dynaxis/knowledge/**
- app/api/dynaxis/projects/**
- tests/dynaxis-project-graph*.test.mjs
- tests/dynaxis-memory*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- memory, knowledge, and decision-record persistence tied to Project Graph source nodes
- service APIs for creating grounded records with source citations, confidence, author, and visibility metadata
- retention/deletion behavior and permission checks for project-scoped memory
- Project Graph extends existing domain tables with typed edges, knowledge, memory, and decision provenance; it must not replace canonical domain persistence. This Work Package is intentionally narrow: it must deliver Memory Knowledge and Decision Records without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- memory, knowledge, and decision-record persistence tied to Project Graph source nodes is implemented, documented, or verified within this package.
- service APIs for creating grounded records with source citations, confidence, author, and visibility metadata is implemented, documented, or verified within this package.

# Validation

- tests proving ungrounded provider chat logs are not accepted as canonical memory
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
