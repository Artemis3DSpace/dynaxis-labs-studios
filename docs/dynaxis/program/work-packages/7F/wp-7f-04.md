---
id: WP-7F-04
phase: 7F
title: Conversation and Provenance Graph Integration
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7F-02", "WP-7F-03"]
blocks: ["WP-7F-05", "WP-7F-06", "WP-7F-07"]
migration_owner: false
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/project-graph/**", "lib/dynaxis/memory/**", "lib/dynaxis/knowledge/**", "app/api/dynaxis/projects/**", "tests/dynaxis-project-graph*.test.mjs", "tests/dynaxis-memory*.test.mjs"]
---

# Objective

Deliver Conversation and Provenance Graph Integration for Phase 7F. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Project Graph extends existing domain tables with typed edges, knowledge, memory, and decision provenance; it must not replace canonical domain persistence. This Work Package is intentionally narrow: it must deliver Conversation and Provenance Graph Integration without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7F-02
- WP-7F-03

# Allowed Paths

- lib/dynaxis/project-graph/**
- lib/dynaxis/memory/**
- lib/dynaxis/knowledge/**
- app/api/dynaxis/projects/**
- tests/dynaxis-project-graph*.test.mjs
- tests/dynaxis-memory*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- conversation node and message provenance model connected to Project Graph records
- ingest service that records source actor, subject, related project/domain nodes, and generated artifacts
- failure behavior for missing project, forbidden conversation, and duplicate provenance events
- Project Graph extends existing domain tables with typed edges, knowledge, memory, and decision provenance; it must not replace canonical domain persistence. This Work Package is intentionally narrow: it must deliver Conversation and Provenance Graph Integration without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- conversation node and message provenance model connected to Project Graph records is implemented, documented, or verified within this package.
- ingest service that records source actor, subject, related project/domain nodes, and generated artifacts is implemented, documented, or verified within this package.

# Validation

- tests for conversation-to-memory and conversation-to-asset graph edges
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
