---
id: WP-8F-02
phase: 8F
title: API Auth Developer Applications and Credentials
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-23", "WP-8F-01"]
blocks: ["WP-8F-03", "WP-8F-04", "WP-8F-05", "WP-8F-06"]
migration_owner: true
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/developer-platform/**", "lib/dynaxis/api-keys/**", "lib/dynaxis/webhooks/**", "app/api/dynaxis/developer/**", "app/api/api/v1/**", "packages/dynaxis-sdk*/**", "scripts/dynaxis-dev*.js", "tests/dynaxis-developer-platform*.test.mjs"]
---

# Objective

Deliver API Auth Developer Applications and Credentials for Phase 8F. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

Developer Platform exposes governed APIs, SDKs, CLI, MCP, webhooks, logs, and sandbox surfaces without bypassing Dynaxis identity or Jobs. This Work Package is intentionally narrow: it must deliver API Auth Developer Applications and Credentials without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-23
- WP-8F-01

# Allowed Paths

- lib/dynaxis/developer-platform/**
- lib/dynaxis/api-keys/**
- lib/dynaxis/webhooks/**
- app/api/dynaxis/developer/**
- app/api/api/v1/**
- packages/dynaxis-sdk*/**
- scripts/dynaxis-dev*.js
- tests/dynaxis-developer-platform*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- Developer Application and developer credential persistence separate from provider credentials and user passwords
- API auth middleware for public API keys/tokens with workspace/project scoping and revocation
- rate-limit and audit hooks for developer API access
- Developer Platform exposes governed APIs, SDKs, CLI, MCP, webhooks, logs, and sandbox surfaces without bypassing Dynaxis identity or Jobs. This Work Package is intentionally narrow: it must deliver API Auth Developer Applications and Credentials without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Developer Application and developer credential persistence separate from provider credentials and user passwords is implemented, documented, or verified within this package.
- API auth middleware for public API keys/tokens with workspace/project scoping and revocation is implemented, documented, or verified within this package.

# Validation

- tests for credential creation, revocation, scope denial, and provider-credential isolation
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
