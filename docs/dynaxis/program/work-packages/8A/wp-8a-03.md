---
id: WP-8A-03
phase: 8A
title: Software Component Contract and Registry
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8A-02"]
blocks: ["WP-8A-04", "WP-8A-06", "WP-8A-07"]
migration_owner: true
type: implementation
recommended_agent: claude
shared_files: ["lib/dynaxis/app-ir/**", "lib/dynaxis/app-factory/**", "lib/dynaxis/software-components/**", "lib/dynaxis/blueprints/**", "lib/dynaxis/repositories/**", "app/api/dynaxis/build/**", "tests/dynaxis-app-factory*.test.mjs"]
---

# Objective

Deliver Software Component Contract and Registry for Phase 8A. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

App Factory Core creates the production Build architecture: App IR, software components, blueprints, capabilities, repositories, and verification state. This Work Package is intentionally narrow: it must deliver Software Component Contract and Registry without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8A-02

# Allowed Paths

- lib/dynaxis/app-ir/**
- lib/dynaxis/app-factory/**
- lib/dynaxis/software-components/**
- lib/dynaxis/blueprints/**
- lib/dynaxis/repositories/**
- app/api/dynaxis/build/**
- tests/dynaxis-app-factory*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- Software Component and Component Version registry with framework compatibility, props/config schema, behavior contract, tests, accessibility, dependencies, license, and provenance
- lookup service that prefers compatible Dynaxis-verified components and records why a component was selected
- separation from Design Components; no shared persistence model
- App Factory Core creates the production Build architecture: App IR, software components, blueprints, capabilities, repositories, and verification state. This Work Package is intentionally narrow: it must deliver Software Component Contract and Registry without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- Software Component and Component Version registry with framework compatibility, props/config schema, behavior contract, tests, accessibility, dependencies, license, and provenance is implemented, documented, or verified within this package.
- lookup service that prefers compatible Dynaxis-verified components and records why a component was selected is implemented, documented, or verified within this package.

# Validation

- tests for version compatibility, required props, license/provenance, and verified-component reuse
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
