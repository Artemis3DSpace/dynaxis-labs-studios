---
id: WP-8A-07
phase: 8A
title: App Factory Core Integration Tests
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-8A-02", "WP-8A-03", "WP-8A-04", "WP-8A-05", "WP-8A-06"]
blocks: ["WP-8B-02", "WP-8B-03", "WP-8D-02", "WP-8D-05"]
migration_owner: false
type: review
recommended_agent: codex
shared_files: ["lib/dynaxis/app-ir/**", "lib/dynaxis/app-factory/**", "lib/dynaxis/software-components/**", "lib/dynaxis/blueprints/**", "lib/dynaxis/repositories/**", "app/api/dynaxis/build/**", "tests/dynaxis-app-factory*.test.mjs"]
---

# Objective

Deliver App Factory Core Integration Tests for Phase 8A. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

App Factory Core creates the production Build architecture: App IR, software components, blueprints, capabilities, repositories, and verification state. This Work Package is intentionally narrow: it must deliver App Factory Core Integration Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-8A-02
- WP-8A-03
- WP-8A-04
- WP-8A-05
- WP-8A-06

# Allowed Paths

- lib/dynaxis/app-ir/**
- lib/dynaxis/app-factory/**
- lib/dynaxis/software-components/**
- lib/dynaxis/blueprints/**
- lib/dynaxis/repositories/**
- app/api/dynaxis/build/**
- tests/dynaxis-app-factory*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- feature implementation beyond test/review fixes
- schema migrations unless explicitly assigned
- unrelated runtime changes

# Required Deliverables

- integration suite covering App IR validation, software component selection, blueprint/capability registry use, repository model, and package import/export
- contract review proving App Factory does not replace Apps Studio, Mini Apps, or Dynaxis Projects
- migration ownership report for all App Factory schema packages
- App Factory Core creates the production Build architecture: App IR, software components, blueprints, capabilities, repositories, and verification state. This Work Package is intentionally narrow: it must deliver App Factory Core Integration Tests without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- integration suite covering App IR validation, software component selection, blueprint/capability registry use, repository model, and package import/export is implemented, documented, or verified within this package.
- contract review proving App Factory does not replace Apps Studio, Mini Apps, or Dynaxis Projects is implemented, documented, or verified within this package.

# Validation

- residual-risk checklist before Build Runtime packages begin
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
