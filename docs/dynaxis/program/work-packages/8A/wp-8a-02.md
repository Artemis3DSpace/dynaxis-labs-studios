---
id: WP-8A-02
phase: 8A
title: App IR Validation and Persistence
status: backlog
agent: unassigned
base_sha: null
branch: null
worktree: null
depends_on: ["WP-7C-23", "WP-7F-07", "WP-7I-05", "WP-8A-01"]
blocks: ["WP-8A-03", "WP-8A-06", "WP-8A-07"]
migration_owner: true
type: implementation
recommended_agent: codex
shared_files: ["lib/dynaxis/app-ir/**", "lib/dynaxis/app-factory/**", "lib/dynaxis/software-components/**", "lib/dynaxis/blueprints/**", "lib/dynaxis/repositories/**", "app/api/dynaxis/build/**", "tests/dynaxis-app-factory*.test.mjs"]
---

# Objective

Deliver App IR Validation and Persistence for Phase 8A. The result must be directly usable by the next assigned coding agent without requiring that agent to invent architecture, schema semantics, service boundaries, or security rules.

# Context

App Factory Core creates the production Build architecture: App IR, software components, blueprints, capabilities, repositories, and verification state. This Work Package is intentionally narrow: it must deliver App IR Validation and Persistence without reinterpreting the roadmap or absorbing adjacent package scope.

# Dependencies

- WP-7C-23
- WP-7F-07
- WP-7I-05
- WP-8A-01

# Allowed Paths

- lib/dynaxis/app-ir/**
- lib/dynaxis/app-factory/**
- lib/dynaxis/software-components/**
- lib/dynaxis/blueprints/**
- lib/dynaxis/repositories/**
- app/api/dynaxis/build/**
- tests/dynaxis-app-factory*.test.mjs
- docs/dynaxis/**

# Forbidden Paths

- unlisted lib/**, app/**, packages/**, drizzle/** paths
- unrelated roadmap phases
- canonical architecture rewrites
- PR creation or merge actions

# Required Deliverables

- App IR revision persistence with application identity, pages/routes, layouts, components, data sources, actions, roles, integrations, deployment targets, and provenance
- versioned validation service with compatibility checks and explicit schema-version errors
- Project Graph links from Project to App IR revisions and verification evidence
- App Factory Core creates the production Build architecture: App IR, software components, blueprints, capabilities, repositories, and verification state. This Work Package is intentionally narrow: it must deliver App IR Validation and Persistence without reinterpreting the roadmap or absorbing adjacent package scope.

# Contracts That Must Not Change

- Dynaxis Project remains the canonical Project; do not create another Project entity.
- Better Auth remains the workspace authentication and organization-membership primitive.
- Provider credentials, model accounts, and worker adapters must not become user identity.
- Dynaxis Jobs, permissions, Project ownership, provenance, and verification gates must not be bypassed.
- Marketplace, App Factory, Skills, and Plugin work must use canonical registries instead of duplicating them.

# Acceptance Criteria

- App IR revision persistence with application identity, pages/routes, layouts, components, data sources, actions, roles, integrations, deployment targets, and provenance is implemented, documented, or verified within this package.
- versioned validation service with compatibility checks and explicit schema-version errors is implemented, documented, or verified within this package.

# Validation

- tests for valid/invalid IR, version migration, and non-React canonical representation
- Run `npm run program:status` when Work Package metadata changes.
- Run `git diff --check` before handoff.

# Commit

Use a scoped commit message beginning with `docs(dynaxis)`, `feat(dynaxis)`, `fix(dynaxis)`, or `test(dynaxis)` as appropriate for the package. Include migration ownership, validation evidence, and any contract deviations in the final handoff.

# Stop Condition

Stop when the deliverables and validation above are complete. If an implementation dependency, migration-owner conflict, security issue, or architecture contradiction appears, stop and report it instead of broadening scope.
